## Git
```bash
git status
git log --oneline
git diff
git init
git add . && git commit -m "message" && git push
git commit -m "message"
git push
```
## Encode
```bash
chcp				# 查看当前编码
chcp 65001			# 修改为UTF-8
chcp 936			# 修改为GBK
```

