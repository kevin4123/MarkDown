## WSL
```bash
wsl --list --online             # 查看可安装
wsl --list --verbose            # 查看已安装
wsl --install -d <发行版名>		# 安装指定发行版

wsl								# 启动
wsl -d <发行版名>				# 启动指定发行版
wsl --setdefault <发行版名>		# 设置默认发行版 

```